import { DragAndDropToolboxDirective } from './drag-and-drop-toolbox.directive';

describe('DragAndDropToolboxDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDropToolboxDirective();
    expect(directive).toBeTruthy();
  });
});
